import React from "react";

const Notification = ({ type = "info", message, onClose }) => {
  return (
    <div>
        <h1> Notification </h1>
      {/* icon  */}
      {/* message */}
      {/* close button */}
    </div>
  );
};

export default Notification;
